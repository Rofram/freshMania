import { ProductFormProps } from '.'

const mock: ProductFormProps = {
  handleResetForm: jest.fn(),
  imageChange: jest.fn(),
  submitButtonLabel: 'cadastrar',
  previewImage: '/img/icon-192.png'
}

export default mock
