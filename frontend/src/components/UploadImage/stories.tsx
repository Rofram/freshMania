import { Story, Meta } from '@storybook/react'
import UploadImage from '.'

export default {
  title: 'UploadImage',
  component: UploadImage
} as Meta

export const Default: Story = () => <UploadImage />
