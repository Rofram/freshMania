import { TooltipProps } from '.'
import { FiAlertCircle } from 'react-icons/fi'

const mock: TooltipProps = {
  title: 'algo de errado aconteceu',
  children: <FiAlertCircle />
}

export default mock
